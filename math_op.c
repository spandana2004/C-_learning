#include<stdio.h>
int main()
{
    int a=2,b=4;
    int sum=a+b;
    int sub=b-a;
    float mul=a*b;
    float div=b/a;
    printf("the sum=%d,subtraction value=%d,product=%f,quotient=%f",sum,sub,mul,div);
    return 0;
}